services:
  - type: web
    name: hermes-revenue-gateway
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: python main.py
    envVars:
      - key: APIFY_TOKEN
        sync: false
